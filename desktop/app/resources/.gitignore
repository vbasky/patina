# Real toolchain/cargo/python are generated by ../prebuild.sh at bundle time
# (hundreds of MB) — never commit them. Keep only the committed placeholders so
# a plain checkout still satisfies tauri.conf's resource globs.
/toolchain/*
/cargo/*
/python/*
!/toolchain/.placeholder
!/cargo/.placeholder
!/python/.placeholder
